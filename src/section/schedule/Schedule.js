import "./Schedule.css";
import Section from "../Section";

function Schedule() {
    return (
        <Section id="schedule">
            schedule
        </Section>
    );
}

export default Schedule;
